// === БАЛАНС МЕТАПРОГРЕССИИ ===
// Все коэффициенты начисления мета-валюты и стоимости узлов собраны здесь,
// чтобы балансить экономику циклов, не трогая логику.
//
// Принцип (plan.md §5.6): первый апгрейд должен быть покупаем уже после 1-го цикла,
// поэтому есть базовое начисление METvalue.base + награда за прожитые дни/убийства/сюжет.

// Монетизация — МЯГКАЯ: бесплатный заработок щедрый, донат лишь ускоряет.
export const META_BALANCE = {
  // --- Начисление очков памяти при смерти ---
  memory: {
    base: 8,             // фикс. начисление за сам факт завершённого цикла (было 5)
    perDay: 0.8,         // за каждый прожитый день (было 0.5)
    perKill: 1.5,        // за каждого убитого врага (было 1)
    perStoryChoice: 4,   // за каждое пройденное сюжетное событие (было 3)
    perElite: 6          // доп. за каждого убитого элитного врага (было 5)
  },
  // --- Начисление ДНК-фрагментов (редкий ресурс) ---
  dna: {
    perElite: 1,         // за каждого убитого элитного врага
    survivalThreshold: 40, // если прожито >= дней — выдать бонусный фрагмент (было 50)
    survivalBonus: 1
  },
  // --- Архивные ключи (триггерный ресурс) ---
  keys: {
    perRecord: 1,        // за побитый рекорд по прожитым дням
    perCyclesMilestone: 5 // +1 ключ каждый N-й цикл (мягкая щедрость)
  }
};
